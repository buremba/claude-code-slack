{
  "name": "@peerbot/orchestrator",
  "version": "1.0.0",
  "description": "KEDA-based orchestrator for peerbot worker management",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.21.0",
    "pg": "^8.11.5",
    "pgboss": "^10.0.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "typescript": "^5.0.0"
  }
}